import styled from 'styled-components';
import { color, space, layout, flexbox, grid, border } from 'styled-system';

const Box = styled('div')(color, space, layout, flexbox, grid, border);

export default Box;
